# Local development uses Vite's same-origin HTTPS proxy, avoiding mixed
# content while the existing backend runs at http://localhost:4000.
VITE_FRAUDLENS_API_URL=/fraudlens-api

# For a hosted build, use the HTTPS backend API prefix, for example:
# VITE_FRAUDLENS_API_URL=https://api.demo.example/api
